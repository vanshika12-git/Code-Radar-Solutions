#include <stdio.h>
int main()
{
    int a,b,c,d,e;
    scanf("%d %d",&a,&b);
    if (c = a+b)
    {
        printf("%d",c);
    }
    else if(c=a-b)
    {
        printf("%d",c);
    }
    return 0 ;
}